# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and should NEVER be committed to version control
#
# Usage:
#   cp .env.example .env.local
#   Edit .env.local with your actual secrets
# =============================================================================

# -----------------------------------------------------------------------------
# TWILIO (Contact Form → WhatsApp Integration)
# -----------------------------------------------------------------------------
# Get these from: https://console.twilio.com/
# Required for: Contact form forwarding to staff WhatsApp
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890
STAFF_WHATSAPP_NUMBER=whatsapp:+1234567890

# -----------------------------------------------------------------------------
# BEDS24 (Booking Management & Payments)
# -----------------------------------------------------------------------------
# Get API keys from: beds24.com
# Account-level key: Account > Settings > API
# Property-level key: Property > Settings > API Key
# Required for: Booking widget integration + API access
# Note: Widget requires PROP_KEY, API access requires both keys
BEDS24_API_KEY=your_beds24_account_api_key_here
BEDS24_PROP_KEY=your_beds24_property_key_here

# -----------------------------------------------------------------------------
# VERCEL BLOB (Image Storage & Optimization)
# -----------------------------------------------------------------------------
# Get token from: Vercel Dashboard > Storage > Blob
# Required for: Image uploads and media management
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# -----------------------------------------------------------------------------
# SENTRY (Error Tracking & Monitoring)
# -----------------------------------------------------------------------------
# Get DSN from: Sentry.io > Project Settings > Client Keys (DSN)
# Required for: Production error monitoring
# Note: DSN is used for both client and server-side error tracking
SENTRY_DSN=your_sentry_dsn_here
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# Optional: Sentry configuration
# SENTRY_ORG=your_org_name
# SENTRY_PROJECT=sumba-sunset

# -----------------------------------------------------------------------------
# GOOGLE ANALYTICS 4 (Analytics & User Tracking)
# -----------------------------------------------------------------------------
# Get Measurement ID from: Google Analytics > Admin > Data Streams
# Required for: User behavior tracking and analytics
# Note: NEXT_PUBLIC_ prefix exposes this to client-side code
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# SITE CONFIGURATION
# -----------------------------------------------------------------------------
# Your production site URL (used for SEO, OG tags, canonical URLs)
# Update this when you have a custom domain
NEXT_PUBLIC_SITE_URL=https://sumba-sunset.com

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS (Optional)
# -----------------------------------------------------------------------------
# Set to 'true' to enable additional debug logging in development
# DEBUG=false

# API rate limiting (optional - for contact form spam prevention)
# RATE_LIMIT_MAX_REQUESTS=5
# RATE_LIMIT_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. For production, add these to Vercel Dashboard > Settings > Environment Variables
# 4. For local development, use .env.local
# 5. Vercel automatically provides some variables (VERCEL_URL, etc.)
